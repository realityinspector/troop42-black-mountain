{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "pwd && ls -la && ls -la dist-server/ 2>&1; ls -la node_modules/.prisma/ 2>&1; echo '--- starting node ---' && node dist-server/index.js 2>&1 || echo 'node exited with code '$?",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 1
  }
}
