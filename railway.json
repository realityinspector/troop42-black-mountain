{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo \"dir: $(ls dist-server 2>&1)\" && echo \"prisma: $(ls node_modules/.prisma/client 2>&1 | head -3)\" && echo \"node: $(node --version 2>&1)\" && node -e \"console.log('test ok')\" && npx prisma db push --skip-generate 2>&1 && node dist-server/index.js 2>&1",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 1
  }
}
